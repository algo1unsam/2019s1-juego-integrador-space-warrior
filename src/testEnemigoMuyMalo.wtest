import enemigos.*
import enemigosMalos.*
import wollok.game.*


//armar describe

const enemigo = new EnemigoMuyMalo(pasos = 1, nivel = 3)

//---------------------------Desplazamiento-------------------------------------

test "Desplazamiento desde la primera posición hacia la segunda"{
	
	enemigo.position(game.at(6,11))
	enemigo.desplazarse()
	
	assert.equals(game.at(8,5), enemigo.position())
}

test "Desplazamiento desde la segunda posición hacia la tercera"{
	
	enemigo.position(game.at(8,5))
	enemigo.desplazarse()
	
	assert.equals(game.at(6,6), enemigo.position())
}

test "Desplazamiento desde la tercera posición hacia la cuarta"{
	
	enemigo.position(game.at(6,6))
	enemigo.desplazarse()
	
	assert.equals(game.at(3,5), enemigo.position())
}

test "Desplazamiento desde la cuarta posición hacia la primera"{
	
	enemigo.position(game.at(3,5))
	enemigo.desplazarse()
	
	assert.equals(game.at(6,11), enemigo.position())
}

